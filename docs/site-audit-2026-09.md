# ことばの地図 全体監査・改善・本番反映

監査日：2026-09-08（JST）。対象：https://kotobanochizu.jp ／ `C:\Projects\hougen`。

## A. Executive Summary

地域・出典・確認範囲を持つ既存データと、地図から県・地域へ進む設計は良い基盤でした。最大の問題は、検索エンジン向けの静的本文と実画面が別実装で、H1の欠落、存在しないパンくず階層、画面遷移時のrobots・構造化データの取り残しが生じていたことです。資料注記が十分表示されず、同じ表記の別地点記録が重複に見える問題もありました。

React画面をビルド時に静的生成する方式に統一し、metadataとパンくずを共有しました。出典の確認範囲・参照日・注記を表示し、地域の閲覧区分と記録地点を区別しました。検索は24件単位に分割し、完全一致を優先。地図のクリック操作を維持し、キーボード・状態・操作説明を改善しました。

本番反映と全2,316 URL再巡回は完了。監査で確認した技術的不整合は解消しています。一方、全資料の言語学的再検証や研究機関の監修を完了したという意味ではありません。出典の取得失敗・不足、分類未確認は明示して保留にしています。

## B. BEFORE / AFTER

母集団はサイトが生成・公開している2,316 URL。404検査用URLや任意の検索パラメータは別途検査しました。数値はGoogleの実際の登録件数ではなく、HTTPとHTMLに基づくindexabilityです。

| 指標 | 変更前本番 | 変更後本番 |
| --- | ---: | ---: |
| 公開URL監査数 | 2,316 | 2,316 |
| HTTP 200ページ | 2,316 | 2,314 |
| index対象 | 1,014 | 1,044 |
| noindexページ | 1,302 | 1,270 |
| 308リダイレクト | 0 | 2 |
| sitemap URL | 1,014 | 1,044 |
| H1欠落・複数 | 265 | 0 |
| metadata欠落 | 0 | 0 |
| canonical不整合 | 0 | 0 |
| 存在しないパンくず参照先 | 3 | 0 |
| 内部リンク切れ | 0 | 0 |
| 孤立ページ | 0 | 0 |
| 重複titleグループ | 17 | 17 |
| 重複descriptionグループ | 8 | 7 |
| index対象同士のmetadata重複 | 0 | 0 |
| テスト | 45成功 | 51成功 |
| lint / typecheck / build | 成功 | 成功 |

変更前のリンク0件は旧静的本文の集計です。実画面では存在しない階層や全件到達性の問題があり、今回は実画面から生成したHTMLでも0件を確認しました。重複metadataは未確認・地域差のあるnoindex記録に残しています。同じ表記だけで統合したり、IDをtitleに足して数字を良くする変更はしていません。

生成HTML総量は約16.9 MBから56.0 MBへ増えました。各ページで画面全体・出典・導線が最初から読めるためであり、56 MBを1訪問で転送する構成ではありません。検索の初期カード数は1,643から24へ削減しました。

## C. 実装内容と優先度

### P0

既存ビルド障害・全体のクロール遮断・データ破壊は認めませんでした。語形・読み・意味・例文・言語分類などの無根拠な変更は行っていません。依存関係のnpm auditは脆弱性0件でした。

### P1（今回解消）

- `seo.ts`のルート台帳を静的HTML・SPAのhead・表示パンくず・JSON-LDで共有。265ページのH1欠落、`/regions`・`/guides`・`/stories`という不存在階層を解消。
- 実際のReact画面を静的生成。旧静的テンプレートと画面の情報差を解消。未定義ページは404画面と実HTTP 404。
- 記録地点と閲覧区分を分け、県境・地域ポリゴンが言語学上の境界を意味しないことを明示。
- 旧公開URLの記録を安全に参照できるようにし、既存正規記録への別名2件だけ308。別地点・別話者の記録は保持。

### P2（今回実装）

- 出典を3件で切らず全件表示。資料名・提供元・確認範囲・参照日・資料注記から根拠をたどれるよう改善。
- 県から資料記事、地域から全収録語への実リンクを整備。資料を創作して一覧を埋めることはしていません。
- 検索の表記・読み・意味の一致順位と24件ページ分割。県・地域等の絞り込み変更でページを戻し、条件をURLに保持。0件時は「未公開」と誤認させず条件に一致しない旨と解除導線を表示。
- 地図47県のEnter/Space操作、aria-pressed、語数の読み上げ名、選択結果へのスキップ、強いfocus輪郭、読込失敗時の補助導線。
- 固定件数67語・171地域を実集計へ変更。資料確認済みと未確認候補が混在する公開プレビューを正確に案内。
- 重複していた複数監査スクリプトの単語公開判定を共通化。全URL監査を再実行できるコマンドと台帳を追加。

## D. index変更

| 判定 | 件数 | 理由 |
| --- | ---: | --- |
| KEEP_INDEX | 1,014 | 既存の公開群を維持 |
| PROMOTE_TO_INDEX | 30 | トップ、編集方針、県一覧、資料付き記録5件以上の県別入口27件 |
| KEEP_NOINDEX | 1,270 | 資料不足・個別評価待ち・地域入口・検索・デモ等の現行判断を維持 |
| DEMOTE_TO_NOINDEX | 0 | 到達失敗だけで資料の誤りと認定しない |
| MERGE / DELETE | 0 | 地域差・話者差を維持 |
| REDIRECT | 2 | `/dialects/d1`→`/dialects/jp-40-fukuoka-011`、`/dialects/d2`→`/dialects/jp-40-fukuoka-016` |

単語630件、意味比較135件、地域ガイド149件、文化ガイド70件、文脈資料30件のindex群は変えていません。県別入口は資料付き記録の比較、地域導線、関連資料のあるグループとして評価しました。195地域ページはnoindexのままです。読み・例文がないことを価値がない理由とはせず、個別資料審査まで昇格を保留しています。全URLの判断は `reports/site-audit/decisions.json` に収録。

## E. 方言データと出典

全国JSONは1,628記録。通常一覧は既存デモ15件を含む1,643記録。47県・195閲覧地域の参照整合はvalidatorとテストで確認しました。出典主要メタデータ不足12件、読み未記載620件、方言例文または標準語訳未記載800件、languageVariety未確認17件は維持。市町村欄未記載123件は、直ちに地域不明とは判断しません（地域・出典の範囲を別途参照）。

出典の一意URL320件をHEAD、失敗時GETで確認し307件を取得できました。HEADで403等となった5件はGETで取得でき、単純なリンク切れ扱いを避けました。広島大学の同一資料を公式移転先で確認し、次の2 URLを13記録で更新しました。語形・意味・参照日は変えていません。

- [大崎下島大長方言の農業従事者の風位語彙について](https://hiroshima.repo.nii.ac.jp/records/2025281)：12記録。
- [岡山市方言における終助詞「デ」](https://hiroshima.repo.nii.ac.jp/records/2031608)：1記録。

未解決13 URLはHTTP 404が8件、接続失敗が5件。影響42記録（うち既存index対象24件）です。既存の書誌・確認履歴を削除せずHOLD_SOURCE_ACCESSとしました。取得成功も内容の再検証完了を意味しません。失敗URL・該当ID・公開状態は `source-holds.json` に記録しています。検索エンジンのキャッシュに資料が残っていても現在取得できない場合は復旧扱いにしていません。

過去のTRUE_DUPLICATE分類8組を再評価。愛媛6組は島・集落・話者・アクセント等、高知1組は梼原町と旧物部村、島根1組は五箇と都万などの注記差があり、全組KEEP_SEPARATE_HOLD_MERGEとしました。原資料の注記を表示することで区別を読めるようにしています。同形異義語・地域差を潰す統合はしていません。琉球諸語を推測で日本語の方言へ変更していません。

## F. UX・アクセシビリティ・性能

本番のトップ・県一覧・長崎県・沖縄県・福岡の地域・与那国島・index単語・noindex単語・検索・404の10画面を390/768/1440 pxで確認。全30ケースでH1は1個、横はみ出し0、名前のないbutton 0。モバイルの地図と単語詳細はスクリーンショットでも確認しました。

本番で47県のSVG操作対象を確認し、長崎県をEnter選択→県詳細へ遷移、metadataとパンくず更新を確認。検索「なおす」4件、「ナマラ」1件、「かわいい」3件、「男鹿市」9件、存在しない文字列0件、次の24件で25〜48件、長崎県で89件・1ページ目へ復帰、対馬で13件を確認しました。既存データ以外の同義語を足していません。

focus・意味のある見出し・ラベル・reduced-motionの既存対応を保持。画面幅検証とキーボード確認は、実機のタッチ操作やスクリーンリーダーによる包括的適合試験の代わりではありません。

検索カードの大量初期描画を解消し、遅延ページもビルド時に本文を生成しました。最大JSチャンクは約375 kB（gzip約58 kB）、React約224 kB（gzip約71 kB）。全国データの同期読み込みは残り、今後の増加時には分割取得と検索設計が必要です。LCP/CLS/INPのフィールド値やLighthouseスコアを今回測定したとはしていません。

## G. Technical SEO

本番全URLのcanonical、og:url、title、description、robots、H1、JSON-LD、パンくず参照先、内部リンク、孤立、index/sitemap整合を機械検査しました。index対象同士のmetadata重複・JSON-LD不正・sitemap重複はいずれも0。

robots.txtはAllow: /と本番sitemapを返し、noindexページもクロール可能。sitemap本体と3分割ファイルは正常でindex対象1,044件のみ収録。2別名は単段308、到達先は200。www・HTTP・末尾スラッシュも正規URLへ308。追跡パラメータ付きトップもindexを維持しcanonicalを正規URLに統一。未知URLは404＋noindexです。

GA4 `G-25ZJS3FXF6` を維持。Search Consoleへの新たな送信や所有権変更は行っていません。取得可能なsitemapを用意し、検索実績の不足で作業を止めていません。

## H. 技術品質

- lint、typecheck、8ファイル51テスト、Viteビルド、静的生成、既存verify-build成功。
- 全1,628件のデータvalidator成功。例文未確認・重複候補等の既存警告は前述のHOLDとして切り分け。
- 単語・意味比較・地域・文化・文脈の各SEO監査、既存SEO全体監査、preview-release-check成功。
- 新しい全URL監査 `npm run audit:site` 成功。本番全URL監査も終了コード0。
- 回帰テストはルート一意性、全パンくず参照、正規化、公開判定、出典不足、redirectの循環防止、表示パンくずとJSON-LDの一致を検証。
- npm audit：脆弱性0件。目的外の依存関係更新・DB migrationはなし。

## I. Production

既存Vercelプロジェクト`kotobanochizu`、scope `catholic-web`の正式CLI手順を使用。

1. ローカル全チェック合格。
2. dryモードで188ファイル・約5.3 MBを確認。環境変数ファイル、ブラウザ情報、tmp、研究・監査成果物、distの内容がアップロードに混入しないことを検査。
3. `deploy --prod --skip-domain --yes`でVercel内の既存本番環境設定を適用。遠隔ビルド成功（生成2,314ページ＋2別名）。
4. 保護された配備URLはVercel CLI認証付き取得で単語の出典・本番canonical・indexと実HTTP 404を確認。
5. `promote`成功。本番ドメインで全2,316 URL再巡回と主要実操作を確認。

配備ID：`dpl_BEdS7i36e6745mSiKFAzV8P8UDN1`。
配備URL：https://kotobanochizu-apik6wwip-catholic-web.vercel.app （既存のアクセス保護あり）。
本番：https://kotobanochizu.jp 。
旧配備：`dpl_CXGEi3rv7Kur7WZyYZPHf4Q1HPGu`（Vercelの復元候補として記録）。

本番環境変数のローカル取得は、秘密情報の保存リスクを理由に自動承認レビューが拒否しました。取得せず、Vercel内の既存設定を使うビルドで完了しています。

## J. HOLD・残課題

| 優先度 | 対象 | 次の解決条件 |
| --- | --- | --- |
| P1 HOLD | 取得失敗13出典URL、42記録 | 同一資料の正式移転先・書誌・該当箇所を確認。既存24 index記録も優先再照合 |
| P1 HOLD | 出典不足12、分類未確認17、旧デモ | 資料と収録範囲を人手で確認。確認済み表示へ勝手に昇格させない |
| P2 HOLD | 8組の重複候補、195地域ページ、読み・例文不足 | 地点・話者・資料の比較と独自価値の編集審査。文字数や件数だけで判定しない |
| P2 | 全国データの同期読込、暮らしの場面タグの表記・粒度 | データの増加時に分割取得・管理されたタグ体系を設計。現在の資料属性を推測で変更しない |
| P2 | 実機・支援技術、Core Web Vitals | 実利用の計測と人手の操作確認。今回のブラウザ幅試験を完全適合認定に使わない |
| P3 | 投稿・訂正の実送信、音声・会話等 | 運営・同意・権利・保存・審査の実装を伴う別工程。現行の端末内デモ表示を維持 |

### 検証資料

`reports/site-audit/production-before.json`と`production-after.json`：本番全URL比較。
`after.json`：ローカル生成HTML（別名用互換HTMLもnoindexとして数えるため1,272件）。
`decisions.json`：2,316 URLの判断台帳。
`sources.json` / `source-url-repairs.json` / `source-holds.json`：出典到達性・修正・保留。
`production-http.json`：robots・sitemap・404・正規化。
`production-browser.json`：主要画面・操作の本番QA記録。
